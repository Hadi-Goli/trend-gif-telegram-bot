<div align="center">
  <img src="pic.jpg" width="150" height="150" style="border-radius: 50%; object-fit: cover; margin-bottom: 20px;" alt="Trend GIF Logo">
  <h1>🎬 ربات تلگرامی Trend GIF</h1>
  <p><strong>یک ربات تلگرامی تمام خودکار و Asynchronous با ادغام FFmpeg، سیستم ارسال گیف توسط کاربران و خط لوله CI/CD.</strong></p>
  
  [![Python](https://img.shields.io/badge/Python-3.11%2B-blue?logo=python&logoColor=white)](https://python.org)
  [![SQLite](https://img.shields.io/badge/SQLite-Database-003B57?logo=sqlite&logoColor=white)](https://sqlite.org)
  [![Docker](https://img.shields.io/badge/Docker-Containerized-2496ED?logo=docker&logoColor=white)](https://docker.com)
  [![FFmpeg](https://img.shields.io/badge/FFmpeg-Media%20Engine-5G6438?logo=ffmpeg&logoColor=white)](https://ffmpeg.org)
  [![Telegram API](https://img.shields.io/badge/PTB-v20+-2CA5E0?logo=telegram&logoColor=white)](https://python-telegram-bot.org/)
  [![GitHub Actions](https://img.shields.io/badge/CI%2FCD-GitHub%20Actions-2088FF?logo=github-actions&logoColor=white)](https://github.com/features/actions)

  <br>
  <i>این متن را به زبان <a href="README.md">انگلیسی (English)</a> بخوانید.</i>
</div>

---

## 📌 معرفی پروژه
یک ربات تلگرامی در سطح تجاری که برای تسهیل فرایند انتشار محتوای رسانه‌ای در کانال‌های تلگرامی طراحی شده است. این ربات فایل‌های ویدیویی و انیمیشن (GIF) را پردازش کرده، واترمارک متنی سفارشی را به صورت پویا با استفاده از **FFmpeg** بر روی آن‌ها اعمال می‌کند و سپس در کانال هدف منتشر می‌سازد. تمامی این فرایندها درون یک محیط ایزوله و امن با استفاده از **داکر (Docker)** و معماری **Webhook** بستر HTTPS اجرا می‌شوند.

همچنین ربات دارای **سیستم ارسال گیف توسط کاربران** است که به کاربران عمومی اجازه می‌دهد گیف‌های خود را برای انتشار در کانال ارسال کنند. ارسالی‌ها از فرآیند بررسی و تأیید ادمین‌ها عبور می‌کنند و در برابر اسپم با محدودیت‌های پلکانی محافظت می‌شوند.

این پروژه نمایانگر مهارت در توسعه بک‌اند مدرن با پایتون، مفاهیم DevOps (همچون داکر و CI/CD) و کار با API های پیچیده است.

## 🚀 ویژگی‌های کلیدی

*   **معماری Webhook ناهمگام (Asynchronous)**: مهاجرت از سیستم کند Polling به یک مدل Webhook پرسرعت و ایمن مناسب برای محیط‌های پروداکشن (Production).
*   **موتور پردازش رسانه**: ادغام یکپارچه با FFmpeg از طریق `asyncio.subprocess` برای دستکاری جریان‌های ویدیویی، اعمال استایل‌های گرافیکی (سایه، فونت) و حذف صدای ویدیوها به صورت کاملاً داینامیک.
*   **رابط کاربری دسته‌بندی‌شده هشتگ‌ها (Categorized UI)**: انتخاب تعاملی و چندگانه هشتگ‌ها با دکمه‌های شیشه‌ای. هشتگ‌ها به صورت هوشمند بر اساس دسته‌بندی جدا می‌شوند و تعداد ستون‌های دکمه‌ها بر اساس طول کلمات برای نمایش ایده‌آل در موبایل، به صورت پویا (Dynamic) تنظیم می‌گردد.
*   **ارسال گیف توسط کاربران**: کاربران عمومی می‌توانند گیف‌های خود را همراه با هشتگ و نام ارسال‌کننده بفرستند. ارسالی‌ها قبل از انتشار توسط ادمین‌ها بررسی می‌شوند.
*   **فرآیند بررسی ادمین‌ها**: ادمین‌ها ارسالی‌ها را در یک گروه اختصاصی با دکمه‌های شیشه‌ای بررسی می‌کنند — قفل‌کردن، ویرایش هشتگ، تأیید یا رد — بدون نیاز به خروج از گروه.
*   **محافظت پلکانی ضد اسپم**: محدودیت نرخ پیشرونده (۱۰ آزاد ← ۳ در ۱۰ دقیقه ← ۱ در ۳۰ دقیقه ← قفل روزانه) برای جلوگیری از سوءاستفاده.
*   **خط لوله CI/CD و DevOps**: استقرار (Deployment) تمام خودکار کدها بر روی سرور از طریق GitHub Actions، به همراه همگام‌سازی فایل‌ها (SCP) و اجرای دستورات (SSH).
*   **محیط کانتینری (Dockerized)**: ربات و تمامی نیازمندی‌های سیستمی آن (مانند ابزار FFmpeg) درون یک کانتینر سبک پکیج شده‌اند تا پایداری و اجرای صحیح روی هر سروری تضمین شود.
*   **ثبت دائم لاگ‌ها (Persistent Logging)**: استفاده از `RotatingFileHandler` پایتون و اتصال آن به Volumeهای داکر جهت ذخیره اصولی لاگ‌ها بدون پر شدن فضای هارد سرور.
*   **مدیریت وضعیت بدون نیاز به FSM**: استفاده هوشمندانه از `context.user_data` برای ردیابی و مدیریت نشست‌های پردازش ویدیو و ارسالی‌های کاربران.
*   **کنترل دسترسی دقیق**: لایه‌های دسترسی سفت‌وسخت به گونه‌ای که تنها شناسه ادمین اصلی (`OWNER_ID`) یا ادمین‌های ثبت‌شده در دیتابیس SQLite مجاز به استفاده از دستورات مدیریتی هستند.

## 🛠️ تکنولوژی‌ها و معماری

- **زبان برنامه‌نویسی:** Python 3.13
- **فریم‌ورک:** `python-telegram-bot` (v20+)
- **پایگاه داده:** SQLite3
- **پردازش مدیا:** FFmpeg
- **زیرساخت:** Docker, Docker Compose
- **استقرار خودکار (CI/CD):** GitHub Actions

### جریان کاری سیستم
```mermaid
graph TD
    subgraph فلوی ادمین
        A[کاربر ادمین] -->|ارسال ویدیو/گیف| B[Webhook ربات تلگرام]
        B -->|بررسی دسترسی| C{آیا ادمین است؟}
        C -->|بله| E[انتخاب هشتگ]
        E -->|تأیید| F[واترمارک FFmpeg]
        F --> G[انتشار در کانال]
    end

    subgraph فلوی ارسال کاربران
        U[کاربر عمومی] -->|/start| B
        B -->|نمایش دکمه ارسال| U
        U -->|ارسال گیف + هشتگ + نام| B
        B -->|بررسی محدودیت| RL{مجاز؟}
        RL -->|خیر| WAIT[پیام صبر کنید]
        RL -->|بله| RG[ارسال به گروه بررسی]
        RG -->|ادمین قفل می‌کند| CLAIM[🔒 قفل روی ادمین]
        CLAIM -->|ویرایش / تأیید / رد| REVIEW{تصمیم}
        REVIEW -->|✅ تأیید| F2[واترمارک FFmpeg]
        F2 --> G2[انتشار + اطلاع‌رسانی به کاربر]
        REVIEW -->|❌ رد| NOTIFY[اطلاع به کاربر]
    end
```

## 📁 ساختار مخزن (Repository)

*   `bot.py`: نقطه ورود اصلی برنامه با استفاده از معماری Webhook.
*   `community.py`: فلوی ارسال گیف توسط کاربران — از دکمه ارسال تا انتخاب هشتگ و تأیید نام.
*   `review.py`: فلوی بررسی ادمین — قفل‌کردن، ویرایش هشتگ، تأیید و رد، تماماً داخل گروه با دکمه‌های شیشه‌ای.
*   `video.py`: خط لوله پردازش رسانه با FFmpeg.
*   `db.py`: مدیریت ارتباط با دیتابیس SQLite، ادمین‌ها، ارسالی‌ها، محدودیت نرخ و لاگ‌ها.
*   `Dockerfile` و `docker-compose.yml`: فایل‌های تنظیمات داکر.
*   `.github/workflows/deploy.yml`: اسکریپت استقرار خودکار کدهای گیت‌هاب اکشنز.

## ⚙️ متغیرهای محیطی (Environment Variables)

برای حفظ امنیت، هیچ رمزی نباید در کدها قرار بگیرد. برای اجرای ربات باید متغیرهای زیر را از طریق فایل `.env` یا بخش Secrets گیت‌هاب اکشن مقداردهی کنید:

| متغیر | توضیحات | مثال |
| :--- | :--- | :--- |
| `BOT_TOKEN` | توکن دریافتی از BotFather | `123456789:ABCDEF...` |
| `IP_ADDRESS` | آی‌پی سرور اصلی شما | `45.91.248.25` |
| `PORT` | پورت شنود Webhook | `8443` |
| `KEYBOARD_MODE` | نوع نمایش دکمه‌ها (`INLINE` شیشه‌ای یا `REPLY` کیبوردی) | `INLINE` |
| `REVIEW_GROUP_ID` | آیدی عددی گروه تلگرامی بررسی ادمین‌ها | `-1001234567890` |

*نکته: برای عملکرد صحیح Webhook روی سرور، فایل‌های گواهینامه SSL یعنی `public.pem` و `private.key` باید در کنار فایل‌های پروژه قرار داشته باشند.*

## 🚀 استقرار روی سرور (CI/CD)

با پوش (Push) کردن کدها به برنچ `main`، یک اکشن در گیت‌هاب به صورت خودکار فعال می‌شود که:
1. از طریق پروتکل SSH به سرور متصل می‌شود.
2. با استفاده از SCP فایل‌های جدید را روی سرور کپی می‌کند.
3. فایل `.env` را با استفاده از Secret های ذخیره‌شده به شکل امن می‌سازد.
4. کانتینرهای داکر را با تغییرات جدید بیلد (Build) و ری‌استارت می‌کند.

## 🛡️ دستورات ادمین اصلی (Owner)
*   `/start`: بررسی وضعیت و سلامت ربات.
*   `/add_admin <user_id>`: افزودن یک شناسه به عنوان ادمین مجاز.
*   `/remove_admin <user_id>`: حذف دسترسی ادمین.
*   `/list_admins`: مشاهده لیست تمامی ادمین‌ها.
*   `/add_tag <hashtag>`: ثبت هشتگ جدید در دسته‌بندی‌ها.
*   `/remove_tag <hashtag>`: حذف هشتگ.
*   `/list_tags`: مشاهده لیست هشتگ‌های فعلی (این دستور برای ادمین‌ها نیز قابل استفاده است).
*   `/report`: دریافت گزارش ماهانه از فعالیت تمامی ادمین‌ها.
*   `/pending`: مشاهده تعداد گیف‌های در انتظار بررسی.

## 📤 ارسال گیف توسط کاربران
*   `/start`: پیام خوش‌آمدگویی با دکمه «📤 ارسال گیف».
*   `/help`: راهنمای استفاده برای ارسال گیف.
*   `/cancel`: لغو فرآیند ارسال فعال.
